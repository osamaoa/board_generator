# Frontend

This folder contains the React and Three.js UI for Board Generator.

Run the API from `backend/`, then start the UI with:

```bash
npm install
npm run dev -- --host 0.0.0.0 --port 5175
```

The UI manual is `../docs/web_app.md`.
